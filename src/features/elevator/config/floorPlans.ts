import type { LevelId } from '@/types/property';

// Add the remaining floor-plan assets here as they arrive.
export const floorPlanImages: Partial<Record<LevelId, string>> = {
  '03': '/images/stock3.jpg',
};
